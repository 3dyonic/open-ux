name: test
on:
  push:
    branches: [master]
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/mcp
    env:
      OPEN_UX_CATALOG: ${{ github.workspace }}/catalog
      OPEN_UX_SCHEMA: ${{ github.workspace }}/catalog/schema.json
      OPEN_UX_WEB_DIST: ${{ github.workspace }}/packages/web/dist
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          package-manager-cache: false
      - name: Build web
        working-directory: packages/web
        run: |
          npm ci
          npm run build
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.12.15"
          enable-cache: true
          cache-dependency-glob: "packages/mcp/uv.lock"
      - name: Install
        run: uv sync --frozen --extra dev
      - name: Validate catalog
        run: uv run python -m open_ux validate-catalog --strict-fit
      - name: Validate Claude plugin
        working-directory: ${{ github.workspace }}
        run: |
          npm install -g @anthropic-ai/claude-code
          claude plugin validate . --strict
          claude plugin validate ./clients/plugin --strict
      - name: Validate Cursor plugin
        working-directory: ${{ github.workspace }}
        run: node scripts/validate-cursor-plugin.mjs --strict
      - name: Test
        run: uv run python -m pytest -q
