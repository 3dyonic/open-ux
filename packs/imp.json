{
  "imp.intent": "UNS-65 + UNS-66 as one PR on master. Ship 40 Actions + 54 Forms from craft-pass SoT only. Extra harvest not Ready — not shipped. No Claude plugin, F3, OAuth, admin UI, QMD/Harbor. Do not merge.",
  "imp.product_name": "Open UX",
  "imp.subtitle": "cited UX rules agents audit against / shared cited guidelines",
  "imp.layout": "packages/mcp (Python FastMCP); catalog/ split by lane (actions.json, forms.json, index.json, schema.json); clients/claude untouched; packs/; docs/",
  "imp.tools": "list_guidelines paged index only (id,title,jobs,lane); search_guidelines query and/or jobs and/or lane same index shape; get_guideline one full body; audit requires jobs or guideline_ids (never whole catalog). Hybrid C, no server LLM. reasons[] = catalog pass_when/fail_when + guideline_id. audit.content never persisted.",
  "imp.catalog": "Lane files merge at load. 40 actions + 54 forms (LIVE seed ×3 first in forms.json). Dual-cite rows kept (placeholder_hint_conflict; required_before_proceed vs submit.enabled_until_request). Index on disk {id,title,jobs,lane} only. Guideline objects match $defs/guideline; lane is index-only. jobs [actions]|[forms]. citation.url clean https URI; Tidwell-only uses O'Reilly Designing Interfaces 3e URI. severity major. Hard ceiling ~256KB. Wording from craft-passes only.",
  "imp.auth": "Unchanged this slice. Hosted invite waitlist/approve/redeem. No OAuth, no admin UI.",
  "imp.privacy": "Never persist audit.content. Keys and invite tokens hashed at rest. Telemetry: callers, tool mix, verdicts, rule ids, target type. Retention 30d. Self-host telemetry off.",
  "imp.deploy": "OPEN_UX_CATALOG now the catalog directory (lane merge). Dockerfile + fly.toml + CI pointed at catalog/. No live URL in this PR.",
  "imp.claude": "Not this slice.",
  "imp.package": "@3dyonic/open-ux (plugin); Python open-ux.",
  "imp.license": "MIT",
  "imp.tests": "index has no pass_when/rule bodies; get one id returns full body; search jobs=actions returns action ids only; search lane=forms returns form ids; audit without scope fails; audit with guideline_ids only those rules; LIVE seed listable; 40+54 loaded no extras.",
  "imp.held": "PR open. Do not merge. Hold for Architect review.",
  "imp.live_url": "",
  "imp.uns44_rule_bodies": "LIVE ×3 kept as first three Forms rows from Forms craft-pass (not rewritten as extras).",
  "imp.claude_registry": "",
  "imp.oauth": ""
}
