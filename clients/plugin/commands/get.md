---
name: get
description: Fetch one Situation Card or one cited guideline.
---

Card → `Open-UX:get_situation`: when, reject, facets, **`leaves: [{ id, count }]`**, merged pointers.
Guideline id → `Open-UX:get_guideline`. Example: `/get design_a_form` then `/pack` that Card or a Leaf. Do not send a file.
