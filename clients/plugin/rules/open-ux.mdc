---
description: Open UX: cited UX criteria, not a second catalog
alwaysApply: true
---

One skill: `open-ux`. First Open UX call: `Open-UX:suggest_situations` or `Open-UX:list_situations`, not `Open-UX:pack` blind. Compose and review share `Open-UX:pack` with `jobs=`. Host returns cited criteria, not pass or fail. Host does not pick a Card. Do not send a file. Card table lives in the open-ux skill. Do not copy it here.
