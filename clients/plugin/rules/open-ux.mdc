---
description: Open UX: cited UX criteria, not a second catalog
alwaysApply: true
---

One skill: `open-ux`. First Open UX call with a task: `Open-UX:suggest_situations` or `Open-UX:list_situations`, not `Open-UX:pack` blind. Bare `/open-ux:open-ux` or invoke with no UI task: [bare-invoke.md](clients/plugin/skills/open-ux/bare-invoke.md) verbatim reply every time. Compose and review share `Open-UX:pack` with `jobs=`. Host returns cited criteria, not pass or fail. Host does not pick a Card. Do not send a file. Card table lives in the open-ux skill. Do not copy it here.
