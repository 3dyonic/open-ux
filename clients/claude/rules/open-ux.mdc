---
description: Open UX — cited UX criteria, not a second catalog
alwaysApply: true
---

One skill: `open-ux`. Compose and review share `Open-UX:audit` with `jobs=`. `jobs=` is a stratified Card sample; `query` is optional attention. Host returns cited criteria, not pass or fail. Do not send a file. Card table lives in the open-ux skill. Do not copy it here.
