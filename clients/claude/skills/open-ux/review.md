# Review

Same skill. Same Cards. Same `audit`. You already have the UI.

1. Pick the Card the work is in (routing table in [SKILL.md](SKILL.md)).
2. `Open-UX:get_situation` with that Card id if you need when / reject / pointers.
3. `Open-UX:audit` with `jobs=<card_id>` or `guideline_ids`. Always scoped.
4. Apply returned `rule` / pass and fail criteria to the artifact you already have.
5. Narrate which ids you used. The host does not take a file and does not return pass or fail.

Do not upload markup, screenshots, or Figma. Do not invent a host verdict.
