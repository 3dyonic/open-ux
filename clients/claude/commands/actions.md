---
description: Actions container — CTAs, delete, leave.
---

Pick the Card from the skill routing table (not a script): `design_actions_and_ctas` or `protect_destructive_and_leave`. Then **must** audit via `clients/claude/skills/open-ux/scripts/audit.py --jobs <card_id>`. Do not send a file.
