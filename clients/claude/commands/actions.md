---
description: Actions container — CTAs, delete, leave.
---

Same `open-ux` skill. Example: delete confirm → `protect_destructive_and_leave` then `Open-UX:pack` with `jobs=protect_destructive_and_leave`. Button labels → `design_actions_and_ctas`. We return the pack. Do not send a file.
