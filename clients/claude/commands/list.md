---
description: List Open UX Situation Cards or a paged guideline index. Never dump the catalog.
---

`$ARGUMENTS` may be a container alias (`forms` / `actions` / `feedback`), `--guidelines`, or empty (Cards).

```
python3 skills/open-ux/scripts/list.py $ARGUMENTS
```

No file. No pass/fail. Narrate ids and titles only. Do not invent ids.
