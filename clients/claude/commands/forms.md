---
description: Forms container — fields, labels, validation, sign-in.
---

Same `open-ux` skill. Example: signup fields → `design_a_form` then `Open-UX:pack` with `jobs=design_a_form`. Validation → `handle_form_errors`. Login → `compose_sign_in`. We return the pack. Do not send a file.
