# Open UX server

Python FastMCP process for the Open UX catalog.

- **Hosted HTTP:** registration required (`uxmcp_` bearer). Tools 401 without a key.
- **Self-host stdio:** no auth, no hosted telemetry.

See the root README for product copy and install path.
