{
  "id": "spectrum.readonly-vs-disabled",
  "category": "Forms",
  "segment": "Field state",
  "title": "readonly vs disabled",
  "rule": "Disabled = field exists but is unavailable. If the value must still be shown and copied, use read-only, not disabled.",
  "citation": {
    "source": "Spectrum — Text field",
    "url": "https://spectrum.adobe.com/page/text-field/"
  },
  "check": "deterministic",
  "pass_when": [
    "Unavailable fields are disabled. Values that must remain visible and copyable are read-only."
  ],
  "fail_when": [
    "A value that must be copied is in a disabled field."
  ],
  "severity": "major",
  "container": "forms_and_input",
  "card": "design_a_form",
  "facet": "wrong_control_for_the_choice",
  "leaf": "use_familiar_control",
  "waive_reason": "agent_fields pending — do not invent from harvest stencil",
  "do_not_claim": "Fluent disabled-label-stays-perceivable as a merge; picker read-only (see `spectrum.picker-readonly-only-if-selection`). Overlap with extra 73: `fluent.disabled-field-label-stays-perceivable`."
}
