{
  "id": "nl.native-required-bubbles-not-error-ui",
  "category": "Forms",
  "segment": "Errors",
  "title": "native required bubbles not error ui",
  "rule": "Native HTML `required` / browser bubbles are not the error UI — insufficient explanation, especially with `pattern`. Prefer custom client errors; `novalidate` once JS loads if `required` was a no-JS fallback. Server-side validation is the guideline. Dual-cite GOLD server-side + error list (not a separate GOLD unique in this 40).",
  "citation": {
    "source": "NL Design System — HTML formuliervalidatie",
    "url": "https://nldesignsystem.nl/richtlijnen/formulieren/foutmeldingen/html-formuliervalidatie/"
  },
  "check": "llm_judgment",
  "pass_when": [
    "With JS, native bubbles are not the error UI (`novalidate` if `required` is a no-JS fallback); errors are custom; server-side validation remains."
  ],
  "fail_when": [
    "Browser `required`/`pattern` bubbles are the only error UI after JS has loaded."
  ],
  "severity": "major",
  "container": "forms_and_input",
  "card": "handle_form_errors",
  "facet": "errors_dont_point_to_the_fix",
  "leaf": "explain_failure_next_to_cause",
  "waive_reason": "agent_fields pending — do not invent from harvest stencil; MULTI_HOME picked explain_failure_next_to_cause; leftover harvest tag recover_from_invalid_input",
  "do_not_claim": "GOV.UK error-summary as this native-bubble ban; an invented GOLD “server-side” unique; WCAG numbers."
}
