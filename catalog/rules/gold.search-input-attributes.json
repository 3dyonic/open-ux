{
  "id": "gold.search-input-attributes",
  "category": "Forms",
  "segment": "Search",
  "title": "search input attributes",
  "rule": "Search input attributes — `autocomplete` on search can leak sensitive queries on shared machines. `autocorrect` / `spellcheck` conflict with server-side search correction. Do not rely on `required` as the only “empty search” message.",
  "citation": {
    "source": "GOLD — Searchbox attributes",
    "url": "https://github.com/designsystemau/gold-design-system-site/blob/main/content/components/searchbox/rationale/attributes.md"
  },
  "check": "deterministic",
  "pass_when": [
    "Search does not use `autocomplete` in a way that stores sensitive queries; `autocorrect`/`spellcheck` are not fighting server-side correction; empty search is not messaged by `required` alone."
  ],
  "fail_when": [
    "Search `autocomplete` stores queries on a shared machine; `required` is the only empty-search message."
  ],
  "severity": "major",
  "container": "forms_and_input",
  "card": "design_a_form",
  "facet": "wrong_control_for_the_choice",
  "leaf": "use_familiar_control",
  "waive_reason": "agent_fields pending — do not invent from harvest stencil",
  "do_not_claim": "Header-search placeholder exception; Suomi visually-hidden search label as this attributes row."
}
