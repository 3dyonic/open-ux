{
  "id": "forms.field_labels.label_stays_visible",
  "category": "Forms",
  "segment": "Field labels",
  "title": "label stays visible",
  "name": "Label stays visible — NN/g",
  "rule": "Keep the label visible while the user types and checks the value; do not rely on text that disappears inside the field.",
  "citation": [
    {
      "source": "NN/g — Placeholders in Form Fields Are Harmful; Material 3 — Text fields",
      "url": "https://www.nngroup.com/articles/form-design-placeholders/"
    }
  ],
  "check": "deterministic",
  "pass_when": [
    "While the field has focus or a value, the label remains on screen."
  ],
  "fail_when": [
    "After typing starts, only the value remains; the naming text has disappeared."
  ],
  "severity": "major",
  "container": "forms_and_input",
  "card": "design_a_form",
  "facet": "field_has_no_lasting_name",
  "leaf": "avoid_placeholder_as_label",
  "overview": "The field name stays on screen while the user types and checks the value.",
  "apply_when": "Labeling signup, settings, or checkout fields.",
  "not_when": "Button/CTA wording. Inline validation after submit — that is handle_form_errors.",
  "agent_hint": "Keep the label visible while the field has a value. Do not rely on text that disappears inside the field.",
  "description": "A label has to remain readable after the user starts typing. While the field has focus or a value, the naming text stays on screen. It fails when only the value remains and the naming text has disappeared. Use this next to visible_label — not for button copy."
}
