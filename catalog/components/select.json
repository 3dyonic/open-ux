{
  "id": "select",
  "title": "Select",
  "overview": "A compact list for one (sometimes many) choice when showing every option on the page would be too long.",
  "apply_when": "Four or more options, settings that can preselect, or a known closed list.",
  "not_when": "Two to five visible options that should be radios; many independent flags (checkboxes).",
  "keywords": [
    "select",
    "dropdown",
    "picker",
    "combobox",
    "<select>",
    "native select",
    "listbox",
    "option",
    "optgroup",
    "multiple"
  ],
  "vs": [
    {
      "id": "radio",
      "why": "short visible set"
    },
    {
      "id": "autocomplete",
      "why": "type to match a large set"
    },
    {
      "id": "list_builder",
      "why": "assemble available vs selected"
    }
  ],
  "variants": {
    "role": [
      {
        "id": "single",
        "overview": "One choice from a compact list.",
        "apply_when": "Four or more options; settings that can preselect.",
        "not_when": "Two to five options that should be radios."
      },
      {
        "id": "multiple",
        "overview": "Many choices from a compact list.",
        "apply_when": "A known closed set they pick several from.",
        "not_when": "Independent flags that should be checkboxes, or a list_builder.",
        "aliases": [
          "select multiple",
          "multi-select"
        ]
      }
    ],
    "size": [
      {
        "id": "large",
        "overview": "Highest in the size hierarchy — the easiest to hit.",
        "apply_when": "The view's main instance, or full-width on a small viewport.",
        "not_when": "A dense toolbar or a third option next to the main control.",
        "aliases": [
          "lg",
          "xl",
          "full-width",
          "block"
        ]
      },
      {
        "id": "medium",
        "overview": "The default size — body use.",
        "apply_when": "Ordinary use in forms, pages, and chrome.",
        "not_when": "The only control on a mobile screen that should span the width.",
        "aliases": [
          "default",
          "md"
        ]
      },
      {
        "id": "small",
        "overview": "Lowest in the size hierarchy — densest.",
        "apply_when": "Inline or chrome that sits beside content.",
        "not_when": "The page's primary instance, or a control under 44×44.",
        "aliases": [
          "sm",
          "xs",
          "compact"
        ]
      }
    ],
    "composition": [
      {
        "id": "native",
        "overview": "The platform select.",
        "apply_when": "A simple closed list.",
        "not_when": "A custom listbox with no keyboard."
      },
      {
        "id": "custom",
        "overview": "A styled listbox / picker.",
        "apply_when": "When the house picker is not native select.",
        "not_when": "A div that looks like a select but is not a listbox.",
        "aliases": [
          "picker",
          "listbox"
        ]
      }
    ],
    "state": [
      {
        "id": "default",
        "overview": "At rest, available, not being used.",
        "apply_when": "The field can take a value now.",
        "not_when": "The field is invalid, disabled, or work is in flight."
      },
      {
        "id": "hover",
        "overview": "Pointer is over the field — extra cue, not the only cue.",
        "apply_when": "A mouse or trackpad can reach the field.",
        "not_when": "Hover is the only way to know it is editable."
      },
      {
        "id": "focus",
        "overview": "Keyboard or assistive tech is on this field. Must stay visible.",
        "apply_when": "Tab, click, or restoring focus after an error jump.",
        "not_when": "Focus outline removed with no replacement.",
        "aliases": [
          "keyboard-focus",
          ":focus-visible"
        ]
      },
      {
        "id": "disabled",
        "overview": "The field is not available. Must stay perceivable and explain why.",
        "apply_when": "A value that must not change on this view.",
        "not_when": "A required answer on a public form — keep it enabled and explain on submit.",
        "aliases": [
          "unavailable",
          "aria-disabled"
        ]
      },
      {
        "id": "error",
        "overview": "The value failed a check. The field stays named and described.",
        "apply_when": "After validate; helper or error text is tied to this field.",
        "not_when": "A page banner with no field association (error_summary without the field).",
        "aliases": [
          "invalid",
          "aria-invalid"
        ]
      },
      {
        "id": "open",
        "overview": "The list is shown.",
        "apply_when": "They are choosing.",
        "not_when": "The list is open and the page behind is unusable with no name.",
        "aliases": [
          "expanded"
        ]
      }
    ]
  },
  "accessibility": [
    {
      "id": "accessible_name",
      "overview": "The field has a name assistive tech can speak — usually a visible label.",
      "apply_when": "Any input, especially placeholder-only or icon-prefixed fields.",
      "not_when": "The only name is placeholder or a tooltip that never reaches the input.",
      "aliases": [
        "aria-label",
        "accessible-name",
        "label"
      ]
    },
    {
      "id": "focus_visible",
      "overview": "The focused field stays visible. Color alone is not the cue.",
      "apply_when": "Keyboard or AT focus is on the field.",
      "not_when": "Outline removed with no replacement.",
      "aliases": [
        "focus-indicator",
        "focus-ring"
      ]
    },
    {
      "id": "aria_disabled",
      "overview": "Unavailable stays in the tab order and explains why.",
      "apply_when": "The field must not run, but focus must not jump away.",
      "not_when": "The HTML disabled attribute, which drops the control from the keyboard flow.",
      "aliases": [
        "aria-disabled"
      ]
    },
    {
      "id": "described_by",
      "overview": "Helper, hint, or error is tied to the field so AT speaks it with the name.",
      "apply_when": "Hint text, character count, or a field error.",
      "not_when": "A page-level message with no aria-describedby / for= on this field.",
      "aliases": [
        "aria-describedby",
        "hint",
        "helper text"
      ]
    },
    {
      "id": "hit_target",
      "overview": "The hit area stays usable, including the label.",
      "apply_when": "Touch, pointer, or a compact form.",
      "not_when": "A 16px box whose tap area is the glyph only.",
      "aliases": [
        "44x44",
        "touch-target",
        "44px"
      ]
    },
    {
      "id": "listbox",
      "overview": "The popup is a listbox (or native select) with option names.",
      "apply_when": "A custom picker.",
      "not_when": "A bunch of buttons with no listbox role."
    }
  ],
  "keyboard": [
    {
      "id": "tab",
      "overview": "Tab lands on the select, then moves on.",
      "apply_when": "Moving through the form.",
      "not_when": "Tab opens the list and traps them."
    },
    {
      "id": "enter",
      "overview": "Enter opens or confirms the focused option.",
      "apply_when": "The select is focused.",
      "not_when": "Enter submits the whole form instead of picking."
    },
    {
      "id": "arrows",
      "overview": "Arrows move through options.",
      "apply_when": "The select or listbox is focused.",
      "not_when": "Arrows scroll the page.",
      "aliases": [
        "arrow keys"
      ]
    },
    {
      "id": "escape",
      "overview": "Escape closes the list without changing the value if that is the house.",
      "apply_when": "The list is open.",
      "not_when": "Escape clears the field."
    },
    {
      "id": "typeahead",
      "overview": "Typing jumps to a matching option.",
      "apply_when": "A long native or listbox list.",
      "not_when": "Typing is swallowed with no match."
    }
  ]
}
